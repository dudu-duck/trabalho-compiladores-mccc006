declarar nota1 = 7.5;
declarar nota2 = 9.0;
declarar nota3 = 6.0;

// Calcula a média ponderada com pesos 2, 3 e 1
declarar media = (nota1 * 2 + nota2 * 3 + nota3);
imprimir(media);

// Calcula um valor de bônus
declarar bonus = media * 100 - 750;
imprimir(bonus);

// Imprime a nota final com bônus
declarar nota_final = media + bonus / 100;
imprimir(nota_final);